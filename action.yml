name: "GeoDiff Action"
description: "GitHub Action for comparing GeoPackage/SQLite files using pygeodiff"
author: "francbartoli"
branding:
  icon: "map"
  color: "blue"

inputs:
  base_file:
    description: "Path to the base GeoPackage/SQLite file"
    required: true
  compare_file:
    description: "Path to the file to compare against base"
    required: true
  output_format:
    description: "Output format for the diff result (json, summary)"
    default: "json"
  summary:
    description: "Add Summary to Job"
    default: "true"
  token:
    description: "GitHub Token"
    default: ${{ github.token }}

outputs:
  diff_result:
    description: "The diff result output"
  has_changes:
    description: "Boolean indicating if changes were detected"

runs:
  using: "docker"
  image: "Dockerfile"
